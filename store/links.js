const linksModule = {
  // ----------------------------------------
  // -- state
  // ----------------------------------------
  state: () => ({}),

  // ----------------------------------------
  // -- getters
  // ----------------------------------------
  getters: {},

  // ----------------------------------------
  // -- mutations
  // ----------------------------------------
  mutations: {},

  // ----------------------------------------
  // -- actions
  // ----------------------------------------
  actions: {
    async create (_, data) {
      await this.$axios.$post('/links', data)
    }
  }
}

export default linksModule
